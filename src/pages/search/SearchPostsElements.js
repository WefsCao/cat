import styled from 'styled-components';

export const SearchResult = styled.div`
  max-width: 80rem;
  margin: 0 auto;
  transform: translateY(15rem);
  font-size: 2rem;
  font-weight: 600;
  margin-bottom: 2rem;
`;
