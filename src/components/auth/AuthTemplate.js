import React from "react";
import styled from "styled-components";

const AuthTemplate = styled.div`
  /* position: relative;
  height: 100vh; */
`;
const AuthTemplateBlock = ({ children }) => {
  return <AuthTemplateBlock>{children}</AuthTemplateBlock>;
};
export default AuthTemplate;
